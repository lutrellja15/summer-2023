#include <iostream>

using namespace std;

int main() {
    int numClasses;

    cout << "How many classes would you like to pick (1-3)? ";
    cin >> numClasses;

    if (numClasses == 1) {
        cout << "You picked one class." << endl;

        string className;
        cout << "Enter the name of the class: ";
        cin.ignore(); // Ignore any remaining newline character
        getline(cin, className);

        cout << "Class selected: " << className << endl;

        if (className == "Math") {
            cout << "You selected Math class." << endl;
            // Code for handling Math class
        } else if (className == "English") {
            cout << "You selected English class." << endl;
            // Code for handling English class
        } else {
            cout << "Unknown class selected." << endl;
            // Code for handling unknown class
        }
    } else if (numClasses == 2) {
        cout << "You picked two classes." << endl;

        string className1, className2;
        cout << "Enter the name of class 1: ";
        cin.ignore(); // Ignore any remaining newline character
        getline(cin, className1);

        cout << "Enter the name of class 2: ";
        getline(cin, className2);

        cout << "Classes selected: " << className1 << ", " << className2 << endl;

        if (className1 == "Science") {
            cout << "You selected Science class." << endl;
            // Code for handling Science class
        } else if (className1 == "History") {
            cout << "You selected History class." << endl;
            // Code for handling History class
        } else {
            cout << "Unknown class selected for class 1." << endl;
            // Code for handling unknown class for class 1
        }

        if (className2 == "Art") {
            cout << "You selected Art class." << endl;
            // Code for handling Art class
        } else if (className2 == "Physical Education") {
            cout << "You selected Physical Education class." << endl;
            // Code for handling Physical Education class
        } else {
            cout << "Unknown class selected for class 2." << endl;
            // Code for handling unknown class for class 2
        }
    } else if (numClasses == 3) {
        cout << "You picked three classes." << endl;

        string className1, className2, className3;
        cout << "Enter the name of class 1: ";
        cin.ignore(); // Ignore any remaining newline character
        getline(cin, className1);

        cout << "Enter the name of class 2: ";
        getline(cin, className2);

        cout << "Enter the name of class 3: ";
        getline(cin, className3);

        cout << "Classes selected: " << className1 << ", " << className2 << ", " << className3 << endl;

        // Code for handling class 1
        if (className1 == "Biology") {
            cout << "You selected Biology class." << endl;
            // Code for handling Biology class
        } else if (className1 == "Physics") {
            cout << "You selected Physics class." << endl;
            // Code for handling Physics class
        } else {
            cout << "Unknown class selected for class 1." << endl;
            // Code for handling unknown class for class 1
        }

        // Code for handling class 2
        if (className2 == "Spanish") {
            cout << "You selected Spanish class." << endl;
            // Code for handling Spanish class
        } else if (className2 == "French") {
            cout << "You selected French class." << endl;
            // Code for handling French class
        } else {
            cout << "Unknown class selected for class 2." << endl;
            // Code for handling unknown class for class 2
        }

        // Code for handling class 3
        if (className3 == "Computer Science") {
            cout << "You selected Computer Science class." << endl;
            // Code for handling Computer Science class
        } else if (className3 == "Economics") {
            cout << "You selected Economics class." << endl;
            // Code for handling Economics class
        } else {
            cout << "Unknown class selected for class 3." << endl;
            // Code for handling unknown class for class 3
        }
    } else {
        cout << "Invalid number of classes. Please pick 1-3." << endl;
        // Code for handling invalid input
    }

    return 0;
}
```

In this example, each nested `if` statement is used to check the name of the class selected by the user within each case. Depending on the class name, specific actions or code sections can be executed accordingly. The code snippets for handling the different classes are included within the appropriate `if` statements.

Feel free to customize the code inside each `if` statement to include your specific handling logic for each class.
